---
phase: 09-real-vision-llm-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/llm/types.ts
  - src/shared/llm/adapters/openai.ts
  - src/shared/llm/adapters/openrouter.ts
  - src/shared/llm/adapters/azure.ts
  - src/shared/llm/adapters/claude.ts
  - src/server/validation/visionValidator.ts
  - src/server/validation/visionValidator.test.ts
autonomous: true
requirements: [VALID-07, VALID-03]
must_haves:
  truths:
    - "La validación visual usa llamada real a LLM con imagen (no heurística pura)."
    - "La validación tiered escala a modelo high solo cuando verdict=contradicts y confidence>0.8."
    - "La salida del auditor es JSON determinista y auditable."
  artifacts:
    - path: "src/server/validation/visionValidator.ts"
      provides: "Validación visual async con provider.complete y parsing JSON estricto"
    - path: "src/shared/llm/types.ts"
      provides: "Contrato multimodal string | ContentPart[]"
    - path: "src/shared/llm/adapters/claude.ts"
      provides: "Traducción image_url data URI a bloque Anthropic base64"
  key_links:
    - from: "src/server/validation/visionValidator.ts"
      to: "src/shared/llm/types.ts"
      via: "LLMRequest/LLMMessage multimodal"
      pattern: "content: string \| ContentPart\[]"
    - from: "src/server/validation/visionValidator.ts"
      to: "provider.complete"
      via: "llamada async real"
      pattern: "await provider\.complete"
---

<objective>
Implementar validación visual real con LLM async y soporte multimodal en adapters/tipos.

Purpose: cumplir VALID-07 y VALID-03 con salida JSON determinista y escalamiento controlado.
Output: validador async + tipos/adapters preparados para imagen.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-real-vision-llm-validation/09-CONTEXT.md
@.planning/phases/09-real-vision-llm-validation/09-RESEARCH.md
@src/server/validation/visionValidator.ts
@src/shared/llm/types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extender contrato LLM multimodal y adapters</name>
  <files>src/shared/llm/types.ts, src/shared/llm/adapters/openai.ts, src/shared/llm/adapters/openrouter.ts, src/shared/llm/adapters/azure.ts, src/shared/llm/adapters/claude.ts</files>
  <read_first>src/shared/llm/types.ts, src/shared/llm/adapters/openai.ts, src/shared/llm/adapters/openrouter.ts, src/shared/llm/adapters/azure.ts, src/shared/llm/adapters/claude.ts, .planning/phases/09-real-vision-llm-validation/09-CONTEXT.md</read_first>
  <behavior>
    - Mensajes LLM aceptan texto o ContentPart[] con image_url data URI.
    - OpenAI/OpenRouter/Azure mantienen paso directo de contenido multimodal.
    - Claude convierte image_url a bloque Anthropic base64.
  </behavior>
  <action>Agregar `ContentPart` y cambiar `LLMMessage.content` a `string | ContentPart[]` (D-01). Añadir soporte opcional de `responseFormat` en `LLMRequest` para `json_object` (D-18). En adapters OpenAI/OpenRouter/Azure, propagar `responseFormat` al body y no aplanar arrays de contenido (D-18). En Claude, adaptar `splitSystemPrompt` y `toClaudePayload` para manejar `ContentPart[]` y traducir data URI a `{ type:'image', source:{ type:'base64', media_type, data } }` (D-03).</action>
  <verify>
    <automated>node ./node_modules/tsx/dist/cli.mjs --test "src/shared/llm/adapters/index.test.ts"</automated>
  </verify>
  <acceptance_criteria>`src/shared/llm/types.ts` exporta `ContentPart`; `LLMMessage.content` ya no es sólo string; adapters OpenAI/OpenRouter/Azure serializan `response_format` cuando existe; `claude.ts` contiene traducción explícita de `image_url` a `source.type: 'base64'`.</acceptance_criteria>
  <done>Contrato multimodal disponible y compilable para todos los providers existentes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reescribir visionValidator a flujo async con JSON estricto y tiering</name>
  <files>src/server/validation/visionValidator.ts, src/server/validation/visionValidator.test.ts</files>
  <read_first>src/server/validation/visionValidator.ts, src/server/validation/visionValidator.test.ts, .planning/phases/09-real-vision-llm-validation/09-CONTEXT.md, .planning/phases/09-real-vision-llm-validation/09-RESEARCH.md</read_first>
  <behavior>
    - `validateStepWithVision` usa `await provider.complete` con imagen base64.
    - Si `stepStatus !== passed`, evita llamada de red y retorna resultado determinista.
    - Si JSON es inválido o falla el proveedor, retorna fallback `uncertain` con `needsReview=true`.
  </behavior>
  <action>Convertir `validateStepWithVision` a async e introducir `VisionValidationInput` con `imageBuffer: Buffer` y `provider: LLMProvider` (D-04). Construir user content multimodal con texto + data URI (D-02). Aplicar temperatura 0 y `responseFormat: { type: 'json_object' }` cuando corresponda (D-18). Mantener regla de tiering: low primero y high sólo con `contradicts` + `confidence > 0.8` (VALID-03). Implementar parseo JSON estricto y fallback de error (D-11, D-13, D-17).</action>
  <verify>
    <automated>node ./node_modules/tsx/dist/cli.mjs --test "src/server/validation/visionValidator.test.ts"</automated>
  </verify>
  <acceptance_criteria>`visionValidator.ts` contiene función async; existe llamada real a `provider.complete`; tests cubren: status failed/aborted sin llamada LLM, JSON inválido -> uncertain/review, y escalamiento high-tier por umbral de confianza.</acceptance_criteria>
  <done>Validación visual ya no depende de heurísticas textuales como mecanismo principal.</done>
</task>

</tasks>

<verification>
`npm run typecheck` pasa sin errores y los tests de validation/adapters quedan verdes.
</verification>

<success_criteria>
- VALID-07 implementado con salida JSON auditable.
- VALID-03 implementado con escalamiento low->high condicionado.
- El validador soporta payload de imagen real en todos los providers activos.
</success_criteria>

<output>
After completion, create `.planning/phases/09-real-vision-llm-validation/09-01-SUMMARY.md`
</output>
