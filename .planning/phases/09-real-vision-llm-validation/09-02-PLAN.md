---
phase: 09-real-vision-llm-validation
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - src/server/runManager.ts
  - src/server/runManager.test.ts
autonomous: true
requirements: [VALID-04, VALID-05, VALID-06]
must_haves:
  truths:
    - "El scorecard marca hallucinated sólo con regla estricta de passed+contradicts+confidence>0.7."
    - "Low confidence (<0.4) queda como NEEDS_REVIEW y no como hallucinated."
    - "El modelo auditor nunca puede ser igual al modelo de orquestación."
  artifacts:
    - path: "src/server/runManager.ts"
      provides: "Wiring del auditorProvider y guard rails de modelo"
    - path: "src/server/runManager.test.ts"
      provides: "Pruebas de igualdad de modelos y wiring de validación"
  key_links:
    - from: "src/server/runManager.ts"
      to: "src/server/validation/visionValidator.ts"
      via: "await validateStepWithVision(...)"
      pattern: "auditorProvider"
    - from: "src/server/runManager.ts"
      to: "src/shared/llm/factory.ts"
      via: "createProvider"
      pattern: "createProvider"
---

<objective>
Conectar runManager al nuevo validador async y reforzar reglas anti-hallucination/model-collision.

Purpose: cumplir VALID-04, VALID-05 y VALID-06 con comportamiento verificable en ejecución real.
Output: runManager con auditorProvider dedicado, checks de modelo y resultados de validación persistentes.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-real-vision-llm-validation/09-CONTEXT.md
@src/server/runManager.ts
@src/server/validation/visionValidator.ts
@src/shared/llm/factory.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Construir auditorProvider en run start y bloquear modelo igual</name>
  <files>src/server/runManager.ts, src/server/runManager.test.ts</files>
  <read_first>src/server/runManager.ts, src/server/runManager.test.ts, .planning/phases/09-real-vision-llm-validation/09-CONTEXT.md</read_first>
  <behavior>
    - `auditorModel` configurable con default `gpt-4.1`.
    - Si auditorModel == model orquestador, el run falla antes de ejecutar pasos.
  </behavior>
  <action>Extender `RunConfig`/input de run para incluir `auditorModel?: string` con default `gpt-4.1` (D-08, D-09). Construir `auditorProvider` usando `createProvider` con mismo provider y modelo auditor (D-07). Implementar guard de colisión de modelo antes de ejecutar escenarios y lanzar error que incluya ambos valores (D-10). Añadir pruebas unitarias del guard y del default.</action>
  <verify>
    <automated>node ./node_modules/tsx/dist/cli.mjs --test "src/server/runManager.test.ts"</automated>
  </verify>
  <acceptance_criteria>`runManager.ts` contiene `auditorModel` default `gpt-4.1`; existe validación explícita de igualdad de modelos antes de ejecución; tests fallan si se remueve ese guard.</acceptance_criteria>
  <done>Se elimina riesgo de circular verdict por usar mismo modelo en auditor y orquestador.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire completo de validateStepWithVision async en trackStepResult</name>
  <files>src/server/runManager.ts, src/server/runManager.test.ts</files>
  <read_first>src/server/runManager.ts, src/server/validation/visionValidator.ts, src/server/runManager.test.ts</read_first>
  <behavior>
    - Se pasa `imageBuffer` real y `auditorProvider` a la validación.
    - Si falla lectura de screenshot, aplica fallback seguro sin tumbar run.
    - Reglas de hallucinated/needsReview se preservan desde StepValidation.
  </behavior>
  <action>Actualizar llamada en `trackStepResult` a `await validateStepWithVision(...)` con `provider`, `imageBuffer`, `stepStatus`, `stepText`, `orchestratorModel` (D-04, D-05). Leer `imageBuffer` desde screenshot guardado; si no está disponible o hay error de lectura, usar ruta de fallback `uncertain + needsReview` y continuar (D-19). Verificar que payload SSE/persistencia mantenga `hallucinated` y `needsReview` según VALID-04/VALID-05.</action>
  <verify>
    <automated>npm test -- src/server/runManager.test.ts src/server/validation/visionValidator.test.ts</automated>
  </verify>
  <acceptance_criteria>`runManager.ts` ya no invoca versión sync de validator; existe `await` en el call-site; los eventos de step incluyen flags de validación coherentes y tests cubren el fallback de screenshot faltante.</acceptance_criteria>
  <done>La ejecución usa dictamen visual real sin romper flujo cuando falta evidencia.</done>
</task>

</tasks>

<verification>
`npm run typecheck` y tests de runManager/validator pasan en conjunto.
</verification>

<success_criteria>
- VALID-04 y VALID-05 reflejados en resultados finales por step.
- VALID-06 aplicado como guard de arranque obligatorio.
- Integración runManager ↔ visionValidator async funcionando sin regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/09-real-vision-llm-validation/09-02-SUMMARY.md`
</output>
