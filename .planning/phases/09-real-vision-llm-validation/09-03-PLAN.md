---
phase: 09-real-vision-llm-validation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/index.ts
  - src/server/index.test.ts
autonomous: true
requirements: [EXEC-07]
must_haves:
  truths:
    - "Al iniciar servidor, las sesiones Browserbase huérfanas en RUNNING se barren una vez."
    - "Sin BROWSERBASE_API_KEY, el server inicia normal y sólo deja log debug."
    - "Fallos parciales de DELETE no bloquean startup (best-effort)."
  artifacts:
    - path: "src/server/index.ts"
      provides: "Rutina de startup sweep Browserbase con fetch"
    - path: "src/server/index.test.ts"
      provides: "Pruebas del sweep: sin key, con sesiones, con fallos parciales"
  key_links:
    - from: "src/server/index.ts"
      to: "https://api.browserbase.com/v1/sessions"
      via: "fetch GET/DELETE"
      pattern: "X-BB-API-Key"
---

<objective>
Agregar barrido de sesiones huérfanas Browserbase en startup sin afectar disponibilidad del backend.

Purpose: cumplir EXEC-07 en entorno cloud MCP y reducir fugas/costos por sesiones RUNNING.
Output: sweep best-effort en arranque + cobertura de tests del comportamiento resiliente.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-real-vision-llm-validation/09-CONTEXT.md
@src/server/index.ts
@src/shared/registry/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar startup sweep Browserbase (best-effort)</name>
  <files>src/server/index.ts</files>
  <read_first>src/server/index.ts, src/shared/registry/index.ts, .planning/phases/09-real-vision-llm-validation/09-CONTEXT.md</read_first>
  <behavior>
    - Sweep corre una sola vez al startup.
    - Si falta API key, skip silencioso (debug).
    - Si falla un DELETE, continúa con el resto y no rompe arranque.
  </behavior>
  <action>Agregar función de sweep en `server/index.ts` usando `fetch` directo a Browserbase API (D-16). Ejecutarla durante `start()` tras inicialización base y antes de listen (D-14). Si no existe `BROWSERBASE_API_KEY`, hacer `logger.debug` y return (D-15). Implementar resumen info `{found, deleted, failed}` y warnings por delete fallido (D-20).</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>`index.ts` contiene llamadas GET a `.../sessions?status=RUNNING` y DELETE por id con header `X-BB-API-Key`; hay logs debug/info/warn según ruta; no existe throw no controlado que detenga el startup por sweep.</acceptance_criteria>
  <done>Sweep de sesiones implementado sin dependencia de SDK externa.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Crear pruebas de sweep resiliente</name>
  <files>src/server/index.test.ts</files>
  <read_first>src/server/index.ts, src/server/runManager.test.ts</read_first>
  <behavior>
    - Test de no-op sin API key.
    - Test de sweep completo con N sesiones.
    - Test de fallo parcial en DELETE sin abortar startup.
  </behavior>
  <action>Crear `src/server/index.test.ts` (Wave 0 implícita para EXEC-07) con fetch mockeado para 3 escenarios: sin key, listado con sesiones y delete exitoso, y delete parcialmente fallido. Validar que la función de sweep resuelva siempre y reporte conteos esperados.</action>
  <verify>
    <automated>node ./node_modules/tsx/dist/cli.mjs --test "src/server/index.test.ts"</automated>
  </verify>
  <acceptance_criteria>Existe archivo `index.test.ts` ejecutable; cubre 3 rutas de control; al simular delete fallido el test confirma que no se lanza excepción fatal y que se continúa con siguientes sesiones.</acceptance_criteria>
  <done>Comportamiento best-effort protegido por pruebas automáticas.</done>
</task>

</tasks>

<verification>
`npm test -- src/server/index.test.ts` pasa y backend sigue arrancando cuando Browserbase falla.
</verification>

<success_criteria>
- EXEC-07 cubierto con sweep startup + resiliencia ante fallos parciales.
- No bloqueo de startup por errores de Browserbase.
</success_criteria>

<output>
After completion, create `.planning/phases/09-real-vision-llm-validation/09-03-SUMMARY.md`
</output>
