---
phase: 10-cli-debug-trace-csv-scorecard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/mcp-bench.ts
  - src/cli/mcp-bench.phase10.contract.test.ts
  - package.json
autonomous: true
requirements: [CLI-03]
must_haves:
  truths:
    - "`mcp-bench debug` imprime por paso la traza completa de tool calls (nombre, args, resultado/error, latencia cuando existe)."
    - "Pasos hallucinated/needs-review se distinguen en terminal con color en header y etiqueta textual fallback."
    - "La salida sigue siendo legible en CI sin ANSI (etiquetas [HALLUCINATED]/[NEEDS-REVIEW])."
  artifacts:
    - path: "src/cli/mcp-bench.ts"
      provides: "Render detallado de toolCalls en runDebug() con truncado y formato estable"
    - path: "package.json"
      provides: "Dependencia directa chalk@4 para resaltado estable"
    - path: "src/cli/mcp-bench.phase10.contract.test.ts"
      provides: "Pruebas de contrato para formato/flags/debug trace"
  key_links:
    - from: "src/cli/mcp-bench.ts"
      to: "src/server/storage/sqlite.ts"
      via: "getRun(runId).steps[].toolCalls"
      pattern: "for \(const toolCall of .*step\.toolCalls"
    - from: "src/cli/mcp-bench.ts"
      to: "step.validation"
      via: "header labels + color"
      pattern: "HALLUCINATED|NEEDS-REVIEW"
---

<objective>
Corregir el modo `debug` del CLI para exponer trazas de tool calls útiles en diagnóstico real de runs.

Purpose: cerrar CLI-03 con salida accionable para debugging en terminal y CI.
Output: render de tool calls por paso + resaltado de flags de validación + pruebas de contrato.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-cli-debug-trace-csv-scorecard/10-CONTEXT.md
@.planning/phases/10-cli-debug-trace-csv-scorecard/10-DISCUSSION-LOG.md
@src/cli/mcp-bench.ts
@src/server/storage/sqlite.ts

<interfaces>
From src/server/storage/sqlite.ts:
```typescript
export interface PersistedStep {
  mcpId: string;
  index: number;
  text: string;
  status: "passed" | "failed" | "aborted";
  message: string;
  tokens: { input: number; output: number; total: number };
  latencyMs: number;
  networkOverheadMs: number;
  toolCalls: unknown[];
  validation: StepValidation | null;
}

export interface RunDetail extends PersistedRun {
  steps: PersistedStep[];
}

export function getRun(runId: string): RunDetail | null;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Definir pruebas de contrato para traza debug y flags visuales</name>
  <files>src/cli/mcp-bench.phase10.contract.test.ts</files>
  <read_first>src/cli/mcp-bench.ts, src/server/storage/sqlite.ts, .planning/phases/10-cli-debug-trace-csv-scorecard/10-CONTEXT.md</read_first>
  <behavior>
    - Cuando `toolCalls` contiene llamadas, el output imprime líneas con prefijo `→`, nombre de tool, args compactos y snippet de result/error.
    - Args se truncán a 200 chars y result/error a 150 chars.
    - Si falta `latencyMs` en toolCall persistido, no explota y omite `lat=` en esa línea.
    - Header incluye etiqueta `[HALLUCINATED]` o `[NEEDS-REVIEW]` cuando aplique.
  </behavior>
  <action>Agregar pruebas de contrato (RED->GREEN) para `runDebug()` per D-01, D-02, D-03, D-04, D-06 y D-07. Mockear `getRun()` con pasos que incluyan: tool call con `result`, tool call con `error`, payloads largos para truncado, y casos con/ sin `latencyMs` para validar fallback seguro.</action>
  <verify>
    <automated>node ./node_modules/tsx/dist/cli.mjs --test "src/cli/mcp-bench.phase10.contract.test.ts"</automated>
  </verify>
  <acceptance_criteria>Las pruebas fallan inicialmente con el render actual y describen explícitamente el contrato de salida requerido por CLI-03.</acceptance_criteria>
  <done>Contrato de salida debug definido y automatizado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar render de tool trace y resaltado con chalk</name>
  <files>src/cli/mcp-bench.ts, package.json</files>
  <read_first>src/cli/mcp-bench.ts, src/cli/mcp-bench.phase10.contract.test.ts, package.json, .planning/phases/10-cli-debug-trace-csv-scorecard/10-CONTEXT.md</read_first>
  <behavior>
    - `runDebug()` imprime bajo cada paso una línea por tool call con formato estable de una sola línea.
    - Header del paso coloreado: rojo para hallucinated, amarillo para needs-review.
    - Etiquetas textuales visibles aun cuando ANSI no esté habilitado.
  </behavior>
  <action>Actualizar `runDebug()` para iterar `step.toolCalls` y renderizar: nombre, `args:` JSON compacto truncado a 200 (D-01), `result:`/`error:` snippet a 150 (D-02), `lat=` sólo si existe (D-03), y prefijo `    → ` (D-04). Incorporar `chalk` como dependencia directa en `package.json` (D-05) y aplicar color sólo al header del paso (D-06), añadiendo etiquetas `[HALLUCINATED]`/`[NEEDS-REVIEW]` siempre (D-07). Evitar tabla multi-línea o pretty JSON para mantener legibilidad CI.
</action>
  <verify>
    <automated>npm test -- src/cli/mcp-bench.phase10.contract.test.ts</automated>
  </verify>
  <acceptance_criteria>La ejecución de `mcp-bench debug` sobre un run con toolCalls muestra cada llamada con los campos esperados y sin regressions en filtrado `--mcp`.</acceptance_criteria>
  <done>CLI-03 satisfecho con salida detallada, robusta y consistente entre terminal interactiva y logs CI.</done>
</task>

</tasks>

<verification>
`npx tsc --noEmit` y pruebas del archivo `src/cli/mcp-bench.phase10.contract.test.ts` pasan; salida debug incluye trazas por tool call y etiquetas de validación.
</verification>

<success_criteria>
- `mcp-bench debug --mcp <id>` imprime trazas de tool call con args/result/error y latencia cuando está disponible.
- Headers de pasos con hallucination/review quedan visualmente identificables y también legibles sin color.
- No hay crash cuando `toolCalls` tenga forma parcial o campos opcionales ausentes.
</success_criteria>

<output>
After completion, create `.planning/phases/10-cli-debug-trace-csv-scorecard/10-01-SUMMARY.md`
</output>
