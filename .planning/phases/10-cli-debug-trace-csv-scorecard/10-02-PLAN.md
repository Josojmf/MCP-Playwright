---
phase: 10-cli-debug-trace-csv-scorecard
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/api/history.ts
  - src/server/api/history.phase10.contract.test.ts
autonomous: true
requirements: [HIST-02]
must_haves:
  truths:
    - "`/api/history/export.csv` entrega una fila por (runId,mcpId) con métricas de scorecard útiles en hojas de cálculo."
    - "`passRate`, `hallucinationCount`, `totalTokens`, `totalCostUsd` salen calculados por MCP en cada run."
    - "El CSV mantiene encabezado estable y formato parseable por Excel/Google Sheets."
  artifacts:
    - path: "src/server/api/history.ts"
      provides: "buildSummaryCsv por (runId,mcpId) y handler export.csv con RunDetail[]"
    - path: "src/server/api/history.phase10.contract.test.ts"
      provides: "Pruebas de contrato para granularidad y columnas scorecard"
  key_links:
    - from: "src/server/api/history.ts"
      to: "src/server/storage/sqlite.ts"
      via: "listRuns() + getRun() para cargar steps por run"
      pattern: "const detail = getRun\(run\.id\)"
    - from: "buildSummaryCsv"
      to: "step.validation.hallucinated"
      via: "aggregación hallucinationCount"
      pattern: "hallucinated"
---

<objective>
Corregir el export de scorecard CSV para que represente desempeño por MCP dentro de cada run.

Purpose: cerrar HIST-02 con CSV analizable y consistente para comparación temporal.
Output: buildSummaryCsv reescrito a grano (runId,mcpId) + pruebas automatizadas de columnas/métricas.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-cli-debug-trace-csv-scorecard/10-CONTEXT.md
@.planning/phases/06-cli-export/06-VERIFICATION.md
@src/server/api/history.ts
@src/server/storage/sqlite.ts

<interfaces>
From src/server/storage/sqlite.ts:
```typescript
export interface RunDetail extends PersistedRun {
  steps: PersistedStep[];
  estimatedCost: number;
}

export interface PersistedStep {
  mcpId: string;
  status: "passed" | "failed" | "aborted";
  tokens: { total: number };
  validation: StepValidation | null;
}

export function listRuns(limit?: number, offset?: number): PersistedRun[];
export function getRun(runId: string): RunDetail | null;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Definir pruebas de contrato para CSV scorecard por (runId,mcpId)</name>
  <files>src/server/api/history.phase10.contract.test.ts</files>
  <read_first>src/server/api/history.ts, src/server/api/history.test.ts, src/server/storage/sqlite.ts, .planning/phases/10-cli-debug-trace-csv-scorecard/10-CONTEXT.md</read_first>
  <behavior>
    - Header exacto: `runId,mcpId,passRate,hallucinationCount,totalTokens,totalCostUsd`.
    - Un run con múltiples MCPs genera múltiples filas (una por mcpId).
    - `passRate` usa `passed/total` por MCP (incluye aborted en denominador) con 2 decimales.
    - `hallucinationCount` y `totalTokens` son sumas por MCP.
  </behavior>
  <action>Crear pruebas RED para endpoint `/api/history/export.csv` y/o `buildSummaryCsv` cubriendo D-08, D-09, D-10, D-11, D-12 y D-15. Incluir casos con varios MCP por run, run sin steps, y caracteres que fuerzan escaping CSV para validar compatibilidad con spreadsheets.</action>
  <verify>
    <automated>node ./node_modules/tsx/dist/cli.mjs --test "src/server/api/history.phase10.contract.test.ts"</automated>
  </verify>
  <acceptance_criteria>Las pruebas describen el contrato nuevo y fallan con la implementación actual per-run.</acceptance_criteria>
  <done>Contrato HIST-02 expresado como pruebas ejecutables.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reescribir agregación CSV y endpoint export.csv con RunDetail[]</name>
  <files>src/server/api/history.ts</files>
  <read_first>src/server/api/history.ts, src/server/api/history.phase10.contract.test.ts, src/server/storage/sqlite.ts, .planning/phases/10-cli-debug-trace-csv-scorecard/10-CONTEXT.md</read_first>
  <behavior>
    - `buildSummaryCsv()` opera sobre detalles de run y agrega por clave (runId,mcpId).
    - `totalCostUsd` se calcula por prorrateo de tokens del run cuando no hay costo por paso (D-14).
    - El endpoint existente `/api/history/export.csv` se mantiene (sin nueva ruta).
  </behavior>
  <action>Actualizar el handler de `/api/history/export.csv` para transformar `PersistedRun[]` en `RunDetail[]` con `getRun(run.id)` (D-08, D-15). Reescribir `buildSummaryCsv` a filas por (runId,mcpId) (D-09), columnas y orden exacto (D-10), cálculo de `passRate` redondeado a 2 decimales con aborted en denominador (D-11), `hallucinationCount` (D-12), `totalTokens` (D-13) y `totalCostUsd` prorrateado por share de tokens del MCP sobre tokens del run, redondeado a 4 decimales (D-14). Evitar introducir nuevas columnas o breaking changes en cabeceras.</action>
  <verify>
    <automated>npm test -- src/server/api/history.phase10.contract.test.ts src/server/api/history.test.ts</automated>
  </verify>
  <acceptance_criteria>El CSV exportado tiene grano y columnas solicitadas, con métricas coherentes por MCP y parseo correcto en imports tabulares.</acceptance_criteria>
  <done>HIST-02 satisfecho con export scorecard por MCP dentro de cada run.</done>
</task>

</tasks>

<verification>
`npm run typecheck` y pruebas de API history pasan; export.csv mantiene ruta y entrega filas por (runId,mcpId) con columnas de scorecard.
</verification>

<success_criteria>
- El endpoint `/api/history/export.csv` produce CSV con encabezado estable y filas por MCP en cada run.
- Métricas `passRate`, `hallucinationCount`, `totalTokens`, `totalCostUsd` son reproducibles a partir de `steps`.
- El archivo abre sin errores de parseo en Excel/Google Sheets.
</success_criteria>

<output>
After completion, create `.planning/phases/10-cli-debug-trace-csv-scorecard/10-02-SUMMARY.md`
</output>
