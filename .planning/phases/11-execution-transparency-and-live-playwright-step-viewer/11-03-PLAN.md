---
phase: 11-execution-transparency-and-live-playwright-step-viewer
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/client/App.tsx
  - src/client/components/run/McpColumnGrid.tsx
autonomous: true
requirements: [UI-04, UI-05, UI-07]
gap_closure: true
must_haves:
  truths:
    - "When a run enters `running`, the live MCP surface becomes the primary visible surface instead of being appended below the pre-run editor and console."
    - "The pre-run setup/editor/status layout is hidden, collapsed, or otherwise removed from the active viewport while `runState === 'running'`."
    - "The live grid mount path does not white-screen in environments where `ResizeObserver` is unavailable or throws."
    - "Suspicious-step rows can render in the live view once the running surface is visible."
  artifacts:
    - path: "src/client/App.tsx"
      provides: "Dedicated running-state surface takeover for the new run screen"
      contains: "runState === \"running\""
    - path: "src/client/components/run/McpColumnGrid.tsx"
      provides: "Guarded width observation with fallback layout behavior"
      contains: "typeof ResizeObserver"
  key_links:
    - from: "src/client/App.tsx"
      to: "src/client/components/run/McpColumnGrid.tsx"
      via: "Running state swaps primary content to live MCP surface"
      pattern: "<McpColumnGrid"
---

<objective>
Close the diagnosed Phase 11 live-view gaps by making the running-state surface take over the active viewport and hardening the live grid against browser/runtime failures in the `ResizeObserver` path.

Purpose: UAT showed that the run actually starts, but users remain on the pre-run console/editor surface or hit a white screen before the live view becomes usable. This plan fixes the running-state presentation and the most likely mount-time crash point.
Output: A resilient live-run experience where the MCP columns are immediately visible while running, and the live subtree no longer fails if `ResizeObserver` is missing or unstable.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-execution-transparency-and-live-playwright-step-viewer/11-UAT.md
@.planning/phases/11-execution-transparency-and-live-playwright-step-viewer/11-01-SUMMARY.md
@src/client/App.tsx
@src/client/components/run/McpColumnGrid.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Promote the live run surface into the primary viewport</name>
  <files>src/client/App.tsx</files>
  <read_first>src/client/App.tsx, .planning/phases/11-execution-transparency-and-live-playwright-step-viewer/11-UAT.md</read_first>
  <behavior>
    - While `runState === "running"`, the main `new_run` content should prioritize the live MCP section instead of leaving the setup/editor + old status panels as the primary visible content.
    - The live run view should feel like a state swap, not an appended section below the fold.
    - The scorecard behavior for `completed` and `aborted` must remain intact.
  </behavior>
  <action>
    Refactor the `activeSection === "new_run"` render branch so the running state gets its own top-level content path.

    Requirements:
    - Render the existing setup/editor/sidebar layout only for non-running states.
    - Render a dedicated running-state layout that makes the live MCP surface immediately visible at the top of the page.
    - Preserve the existing `Live Console` and essential run metadata only if they support the running experience, but do not leave them ahead of the live grid in the page flow.
    - Keep the existing completed/aborted scorecard swap behavior working after the run finishes.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/client/App.tsx` has a distinct `runState === "running"` render path for the new-run screen.
    - The pre-run editor/setup layout is not left as the primary visible content during active runs.
    - `<McpColumnGrid />` is rendered at the top of the running-state experience.
    - The `completed` / `aborted` scorecard path still renders after a run ends.
  </acceptance_criteria>
  <done>The running-state view takes over the main viewport and reveals the live MCP surface immediately.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Harden McpColumnGrid against ResizeObserver failures</name>
  <files>src/client/components/run/McpColumnGrid.tsx</files>
  <read_first>src/client/components/run/McpColumnGrid.tsx</read_first>
  <behavior>
    - The live grid must not crash if `ResizeObserver` is missing, unsupported, or throws.
    - The layout should fall back safely to a predictable default when width observation is unavailable.
  </behavior>
  <action>
    Guard the `ResizeObserver` usage in `McpColumnGrid.tsx`.

    Requirements:
    - Check for API availability before constructing `new ResizeObserver(...)`.
    - Add a fallback container-width path if the observer cannot be attached.
    - Keep the grid-vs-tabs decision deterministic even without the observer.
    - Do not regress the existing tabs fallback behavior for 3+ MCPs or narrow layouts.
  </action>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/client/components/run/McpColumnGrid.tsx` no longer assumes `ResizeObserver` always exists.
    - The component can render without throwing even when width observation is unavailable.
    - The live grid still supports both grid and tab fallback modes.
  </acceptance_criteria>
  <done>The live grid mount path is resilient and no longer presents a likely white-screen failure point.</done>
</task>

</tasks>

<verification>
`npm run typecheck` passes. `npm run build` passes. Manual rerun of Phase 11 UAT confirms the live run surface is visible while running and suspicious-step rows can be inspected.
</verification>

<success_criteria>
- The running state reveals the live MCP surface immediately.
- The pre-run layout no longer obscures the live execution experience.
- `ResizeObserver` no longer acts as a single point of failure for the live view.
- Test 1 and Test 3 from `11-UAT.md` become re-testable and passable.
</success_criteria>

<output>
After completion, create `.planning/phases/11-execution-transparency-and-live-playwright-step-viewer/11-03-SUMMARY.md`
</output>
