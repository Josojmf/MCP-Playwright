---
phase: 11-execution-transparency-and-live-playwright-step-viewer
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/client/components/run/ScreenshotLightbox.tsx
  - src/client/components/ui/dialog.tsx
autonomous: true
requirements: [UI-05, UI-06, UI-07]
gap_closure: true
must_haves:
  truths:
    - "Clicking a live screenshot opens a visible lightbox modal with the full-size screenshot and the expected dark overlay."
    - "The lightbox uses the app’s dialog anatomy correctly instead of bypassing the shared content wrapper."
    - "Closing the lightbox returns the user to the live run screen cleanly."
  artifacts:
    - path: "src/client/components/run/ScreenshotLightbox.tsx"
      provides: "Correct modal content structure for live screenshot preview"
      contains: "DialogContent"
    - path: "src/client/components/ui/dialog.tsx"
      provides: "Any minimal shared-dialog support needed by the lightbox"
      contains: "DialogContent"
  key_links:
    - from: "src/client/components/run/McpColumn.tsx"
      to: "src/client/components/run/ScreenshotLightbox.tsx"
      via: "Clicking the live screenshot opens the corrected modal"
      pattern: "onScreenshotClick"
---

<objective>
Fix the Phase 11 lightbox regression by rebuilding the live screenshot modal with the app’s shared dialog anatomy so the screenshot content renders correctly instead of presenting a blank/white screen.

Purpose: UAT confirmed that the screenshot click path opens a broken modal experience even though the thumbnail and screenshot API path are valid. This plan repairs the dialog composition and restores the live screenshot preview behavior.
Output: A working screenshot lightbox with a visible image, dark overlay, and clean close behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-execution-transparency-and-live-playwright-step-viewer/11-UAT.md
@src/client/components/run/ScreenshotLightbox.tsx
@src/client/components/ui/dialog.tsx
@src/client/components/run/McpColumn.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rebuild ScreenshotLightbox with correct dialog composition</name>
  <files>src/client/components/run/ScreenshotLightbox.tsx, src/client/components/ui/dialog.tsx</files>
  <read_first>src/client/components/run/ScreenshotLightbox.tsx, src/client/components/ui/dialog.tsx, src/client/components/run/McpColumn.tsx</read_first>
  <behavior>
    - The lightbox must show the full-size screenshot inside a visible modal content surface.
    - The overlay remains dark and non-blurred per the UI spec.
    - Close behavior returns to the live view without breaking the run.
  </behavior>
  <action>
    Replace the custom manual Base UI `Backdrop` + `Popup` assembly in `ScreenshotLightbox.tsx` with the app’s shared dialog content pattern, or make the minimum shared-dialog adjustment needed so the lightbox follows the correct content/container anatomy.

    Requirements:
    - Reuse the existing app dialog abstraction where possible.
    - Preserve the required overlay styling and close control.
    - Keep screenshot URL wiring unchanged unless the modal fix proves the path is wrong.
    - Avoid introducing a second dialog pattern that diverges from the rest of the app.
  </action>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `ScreenshotLightbox.tsx` uses a correct dialog content/container composition.
    - Clicking a live screenshot renders the modal image content instead of a white/blank screen.
    - Closing the modal returns to the live run screen cleanly.
  </acceptance_criteria>
  <done>The live screenshot lightbox renders correctly and matches the Phase 11 UI contract.</done>
</task>

</tasks>

<verification>
`npm run typecheck` passes. `npm run build` passes. Manual rerun of Phase 11 UAT confirms Test 2 passes with a visible screenshot modal.
</verification>

<success_criteria>
- The live screenshot thumbnail opens a usable lightbox.
- The modal follows the shared dialog anatomy instead of an ad hoc modal structure.
- Test 2 from `11-UAT.md` becomes re-testable and passable.
</success_criteria>

<output>
After completion, create `.planning/phases/11-execution-transparency-and-live-playwright-step-viewer/11-04-SUMMARY.md`
</output>
