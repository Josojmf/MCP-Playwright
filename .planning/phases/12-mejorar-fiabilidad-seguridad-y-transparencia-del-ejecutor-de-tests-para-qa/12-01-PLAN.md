---
phase: 12-mejorar-fiabilidad-seguridad-y-transparencia-del-ejecutor-de-tests-para-qa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/validation/assertionsRunner.ts
  - src/server/validation/assertionsRunner.test.ts
  - src/server/parser/assertionPatterns.ts
autonomous: true
requirements: [VALID-02]
must_haves:
  truths:
    - "`src/server/validation/assertionsRunner.ts` no contiene `new Function(` ni `eval(` para ejecutar assertions traducidas."
    - "`runAssertion()` solo autoejecuta patterns allowlisted por `patternId` y falla de forma explicita cuando `patternId` es `null` o no existe runner auditable."
    - "Los diez patterns actuales de `src/server/parser/assertionPatterns.ts` siguen teniendo ejecucion determinista via Playwright `expect` sin pasar por evaluacion dinamica."
  artifacts:
    - path: "src/server/validation/assertionsRunner.ts"
      provides: "Tabla allowlisted de runners auditable por patternId y mensajes explicitos para shapes no soportados"
    - path: "src/server/validation/assertionsRunner.test.ts"
      provides: "Cobertura de success/failure para patterns soportados y rechazo de paths no allowlisted"
    - path: "src/server/parser/assertionPatterns.ts"
      provides: "Fuente de verdad de los patternId que el runner puede aceptar"
  key_links:
    - from: "src/server/parser/assertionPatterns.ts"
      to: "src/server/validation/assertionsRunner.ts"
      via: "TranslatedAssertion.patternId"
      pattern: "id: \"url-match\"|\"title-match\"|\"text-visible\""
    - from: "src/server/validation/assertionsRunner.ts"
      to: "src/server/validation/assertionsRunner.test.ts"
      via: "runAssertion(assertion, context)"
      pattern: "Unsupported translated assertion pattern|ALLOWLISTED_ASSERTION_RUNNERS"
---

<objective>
Remove arbitrary-code assertion execution from the validation path and replace it with a strict allowlisted Playwright runner keyed by translated assertion pattern IDs.

Purpose: harden the primary security boundary called out in Phase 12 without regressing the existing VALID-02 behavior.
Output: auditable pattern runners plus regression tests proving unsupported shapes do not execute.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-mejorar-fiabilidad-seguridad-y-transparencia-del-ejecutor-de-tests-para-qa/12-CONTEXT.md
@src/server/parser/assertionPatterns.ts
@src/server/parser/translator.ts
@src/server/validation/assertionsRunner.ts
@src/server/validation/assertionsRunner.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace string evaluation with an explicit runner table keyed by patternId</name>
  <files>src/server/validation/assertionsRunner.ts</files>
  <read_first>src/server/validation/assertionsRunner.ts, src/server/parser/assertionPatterns.ts, src/server/parser/translator.ts, .planning/phases/12-mejorar-fiabilidad-seguridad-y-transparencia-del-ejecutor-de-tests-para-qa/12-CONTEXT.md</read_first>
  <action>Replace `executePlaywrightAssertion(playwrightCall, context)` with an allowlisted map named `ALLOWLISTED_ASSERTION_RUNNERS` keyed by the current translator IDs: `url-match`, `title-match`, `text-visible`, `locator-visible`, `locator-contains-text`, `count-elements`, `attribute-value`, `input-value`, `redirect-url`, and `text-content`. `runAssertion()` must branch on `assertion.patternId`, call the matching runner with `context.page` and `context.expect`, and return `{ status: 'failed', message: 'Unsupported translated assertion pattern: <patternId or untranslatable>' }` when the pattern is missing, null, or not allowlisted. Delete the dynamic `new Function('expect', 'page', ...)` path entirely.</action>
  <verify>
    <automated>node ./node_modules/tsx/dist/cli.mjs --test "src/server/validation/assertionsRunner.test.ts"</automated>
  </verify>
  <acceptance_criteria>`src/server/validation/assertionsRunner.ts` contains `ALLOWLISTED_ASSERTION_RUNNERS`; `src/server/validation/assertionsRunner.ts` does not contain `new Function(`; `runAssertion(` checks `assertion.patternId` before execution; unsupported patterns return a message containing `Unsupported translated assertion pattern`.</acceptance_criteria>
  <done>Assertion execution is explicit, reviewable, and no longer relies on arbitrary code evaluation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add regression tests for supported patterns, unsupported patterns, and Spanish failure messages</name>
  <files>src/server/validation/assertionsRunner.test.ts</files>
  <read_first>src/server/validation/assertionsRunner.test.ts, src/server/validation/assertionsRunner.ts, src/server/parser/assertionPatterns.ts, .planning/phases/12-mejorar-fiabilidad-seguridad-y-transparencia-del-ejecutor-de-tests-para-qa/12-CONTEXT.md</read_first>
  <action>Update `src/server/validation/assertionsRunner.test.ts` so it stops asserting behavior through raw `playwrightCall` strings alone and instead covers: one passing allowlisted URL/title/text pattern, one failing allowlisted pattern with a translated Spanish error, one `patternId: null` case, and one synthetic unsupported `patternId: 'custom-eval'` case that must fail without executing any user-provided string. Add a source-level contract assertion that the runner file does not contain `new Function(`.</action>
  <verify>
    <automated>node ./node_modules/tsx/dist/cli.mjs --test "src/server/validation/assertionsRunner.test.ts"</automated>
    <automated>node ./node_modules/typescript/bin/tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>`src/server/validation/assertionsRunner.test.ts` asserts a `patternId: 'custom-eval'` failure path; the test suite passes via the single-file `tsx --test` command; at least one test checks for the literal message fragment `Unsupported translated assertion pattern`; `tsc --noEmit` exits 0.</acceptance_criteria>
  <done>The hardening contract is locked down by tests and future regressions back to dynamic execution are detectable.</done>
</task>

</tasks>

<verification>
`node ./node_modules/tsx/dist/cli.mjs --test "src/server/validation/assertionsRunner.test.ts"` and `node ./node_modules/typescript/bin/tsc --noEmit` pass; source inspection confirms no dynamic evaluation remains in `assertionsRunner.ts`.
</verification>

<success_criteria>
- `runAssertion()` executes only explicit pattern runners keyed by `TranslatedAssertion.patternId`.
- Unsupported or untranslated assertions fail clearly instead of executing arbitrary code.
- Existing supported assertion coverage remains intact through automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/12-mejorar-fiabilidad-seguridad-y-transparencia-del-ejecutor-de-tests-para-qa/12-01-SUMMARY.md`
</output>
