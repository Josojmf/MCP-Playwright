---
phase: 12-mejorar-fiabilidad-seguridad-y-transparencia-del-ejecutor-de-tests-para-qa
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/client/App.tsx
  - src/server/index.ts
  - src/server/runManager.ts
  - src/server/runManager.test.ts
  - src/server/runManager.phase12.contract.test.ts
autonomous: true
requirements: [EXEC-04, UI-04]
must_haves:
  truths:
    - "`/api/runs/estimate` y `/api/runs/start` aceptan `provider`, `orchestratorModel`, `lowCostAuditorModel`, y `highAccuracyAuditorModel` como inputs explicitos del run."
    - "`PhaseOneRunManager` deja de usar la autodeteccion por entorno como fuente de verdad cuando el request ya trae provider/modelos; el entorno solo aporta secretos o defaults iniciales."
    - "La UI de nuevo run muestra y persiste los cuatro campos de configuracion reproducible antes de estimar o ejecutar."
  artifacts:
    - path: "src/server/runManager.ts"
      provides: "Contrato normalizado de run config y validaciones de igualdad entre orquestador y auditors"
    - path: "src/server/index.ts"
      provides: "Endpoints estimate/start alineados con el nuevo payload explicito"
    - path: "src/client/App.tsx"
      provides: "Controles de provider/modelos y requestBody reproducible"
    - path: "src/server/runManager.phase12.contract.test.ts"
      provides: "Prueba de contrato del payload y de la eliminacion de inferencia silenciosa"
  key_links:
    - from: "src/client/App.tsx"
      to: "src/server/index.ts"
      via: "POST /api/runs/estimate y /api/runs/start"
      pattern: "provider|orchestratorModel|lowCostAuditorModel|highAccuracyAuditorModel"
    - from: "src/server/index.ts"
      to: "src/server/runManager.ts"
      via: "RunEstimateRequest"
      pattern: "estimateRun(request.body as RunEstimateRequest)|createRun(request.body as RunEstimateRequest)"
---

<objective>
Make the QA run configuration explicit and reproducible across UI and backend instead of silently inferring provider and models from ambient environment state.

Purpose: satisfy the Phase 12 requirement that QA can choose and later reproduce the exact execution stack used for a run.
Output: explicit request contract, UI controls, and contract tests for provider/model handling.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-mejorar-fiabilidad-seguridad-y-transparencia-del-ejecutor-de-tests-para-qa/12-CONTEXT.md
@src/client/App.tsx
@src/server/index.ts
@src/server/runManager.ts
@src/server/runManager.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace the ambiguous run request shape with explicit provider and model fields</name>
  <files>src/server/runManager.ts, src/server/index.ts, src/server/runManager.test.ts, src/server/runManager.phase12.contract.test.ts</files>
  <read_first>src/server/runManager.ts, src/server/index.ts, src/server/runManager.test.ts, src/server/mcp/preflight.ts, .planning/phases/12-mejorar-fiabilidad-seguridad-y-transparencia-del-ejecutor-de-tests-para-qa/12-CONTEXT.md</read_first>
  <action>Change `RunEstimateRequest` so the explicit payload keys are `provider`, `orchestratorModel`, `lowCostAuditorModel`, and `highAccuracyAuditorModel` instead of the current ambiguous `model` field. `NormalizedRunEstimateRequest` must require all four values after normalization. Keep env lookup only as a fallback when the request omits them, but stop re-detecting provider inside `createRun()` once the normalized request already resolved it. Update `session.config` to store the exact normalized values and emit them in the `run_started` payload under `executionConfig: { provider, orchestratorModel, lowCostAuditorModel, highAccuracyAuditorModel }`. Add `src/server/runManager.phase12.contract.test.ts` to assert those literals are present in the request contract and that `createRun()` does not overwrite them with env-driven autodetection.</action>
  <verify>
    <automated>node ./node_modules/tsx/dist/cli.mjs --test "src/server/runManager.test.ts" "src/server/runManager.phase12.contract.test.ts"</automated>
  </verify>
  <acceptance_criteria>`src/server/runManager.ts` defines `orchestratorModel`; `src/server/runManager.ts` emits `executionConfig` in `run_started`; `src/server/runManager.phase12.contract.test.ts` exists and matches the literal field names `provider`, `orchestratorModel`, `lowCostAuditorModel`, and `highAccuracyAuditorModel`; the targeted `tsx --test` command exits 0.</acceptance_criteria>
  <done>The backend contract now carries reproducible execution choices explicitly and predictably.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add explicit run configuration controls to the New Run UI and persist them locally</name>
  <files>src/client/App.tsx</files>
  <read_first>src/client/App.tsx, src/server/runManager.ts, .planning/phases/12-mejorar-fiabilidad-seguridad-y-transparencia-del-ejecutor-de-tests-para-qa/12-CONTEXT.md</read_first>
  <action>Extend the top-level App state and `STORAGE_KEY` persistence to include `provider`, `orchestratorModel`, `lowCostAuditorModel`, and `highAccuracyAuditorModel`. In the `Contexto de ejecucion` panel, add one provider `<select>` with the exact options `openai`, `claude`, `azure`, and `openrouter`, plus three text inputs labeled `Modelo orquestador`, `Auditor low-cost`, and `Auditor high-accuracy`. Use these exact keys in `requestBody` for both `/api/runs/estimate` and `/api/runs/start`, and include the selected values in the log message or run summary state so the live UI can reference them immediately after start.</action>
  <verify>
    <automated>node ./node_modules/typescript/bin/tsc --noEmit</automated>
    <automated>node ./node_modules/vite/bin/vite.js build</automated>
  </verify>
  <acceptance_criteria>`src/client/App.tsx` contains the labels `Modelo orquestador`, `Auditor low-cost`, and `Auditor high-accuracy`; `requestBody` includes the four explicit config keys; `localStorage.setItem` persists those keys under `STORAGE_KEY`; `tsc --noEmit` and `vite build` exit 0.</acceptance_criteria>
  <done>The UI no longer launches QA runs from hidden model/provider assumptions.</done>
</task>

</tasks>

<verification>
`node ./node_modules/tsx/dist/cli.mjs --test "src/server/runManager.test.ts" "src/server/runManager.phase12.contract.test.ts"`, `node ./node_modules/typescript/bin/tsc --noEmit`, and `node ./node_modules/vite/bin/vite.js build` pass; source inspection shows the exact run config keys present in both UI and backend request handling.
</verification>

<success_criteria>
- The explicit run payload includes provider, orchestrator model, and both auditor models end to end.
- The UI exposes those inputs before estimation and execution.
- The backend keeps the normalized request values as the source of truth for the run session.
</success_criteria>

<output>
After completion, create `.planning/phases/12-mejorar-fiabilidad-seguridad-y-transparencia-del-ejecutor-de-tests-para-qa/12-02-SUMMARY.md`
</output>
