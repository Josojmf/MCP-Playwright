---
phase: 12-mejorar-fiabilidad-seguridad-y-transparencia-del-ejecutor-de-tests-para-qa
plan: 03
type: execute
wave: 2
depends_on: [12-02]
files_modified:
  - src/server/orchestrator/types.ts
  - src/server/mcp/InstrumentedMcpClient.ts
  - src/server/mcp/InstrumentedMcpClient.test.ts
  - src/server/runManager.ts
  - src/server/storage/sqlite.ts
  - src/server/storage/sqlite.test.ts
  - src/server/api/history.ts
  - src/server/api/history.test.ts
autonomous: true
requirements: [VALID-01, HIST-01, HIST-02]
must_haves:
  truths:
    - "`src/server/runManager.ts` deja de persistir screenshots placeholder 1x1 y solo marca el run como `degraded` cuando falta evidencia real."
    - "SQLite guarda `trustState`, `trustReasons`, `provider`, `orchestratorModel`, `lowCostAuditorModel`, y `highAccuracyAuditorModel` en runs persistidos."
    - "Cada step persistido conserva tool traces estructurados con `toolName`, `arguments`, `status`, `latencyMs`, `correlationId`, `screenshotId`, `result`, y `error`."
  artifacts:
    - path: "src/server/orchestrator/types.ts"
      provides: "Contrato comun de ToolCallTrace con campos de trazabilidad y screenshot linkage"
    - path: "src/server/runManager.ts"
      provides: "Promocion de evidence/trust metadata desde la ejecucion en memoria hasta el run persistido"
    - path: "src/server/storage/sqlite.ts"
      provides: "Schema y mapeo persistido de trust state, config reproducible y tool trace enriquecido"
    - path: "src/server/api/history.ts"
      provides: "JSON/CSV export con trust/config/evidence metadata"
  key_links:
    - from: "src/server/mcp/InstrumentedMcpClient.ts"
      to: "src/server/orchestrator/types.ts"
      via: "ToolCallTrace"
      pattern: "correlationId|latencyMs|screenshotId"
    - from: "src/server/runManager.ts"
      to: "src/server/storage/sqlite.ts"
      via: "saveRun(...) y saveScreenshot(...)"
      pattern: "trustState|trustReasons|toolCalls"
    - from: "src/server/storage/sqlite.ts"
      to: "src/server/api/history.ts"
      via: "PersistedRun y RunDetail"
      pattern: "provider|orchestratorModel|highAccuracyAuditorModel"
---

<objective>
Persist trustworthy execution evidence instead of ephemeral in-memory traces and placeholder screenshots, and store an explicit auditable-vs-degraded trust record with each run.

Purpose: make QA history, exports, and post-run review answerable from durable data rather than logs or process memory.
Output: enriched trace contract, real screenshot persistence, run trust state, and history/export schema updates.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-mejorar-fiabilidad-seguridad-y-transparencia-del-ejecutor-de-tests-para-qa/12-CONTEXT.md
@src/server/orchestrator/types.ts
@src/server/mcp/InstrumentedMcpClient.ts
@src/server/mcp/InstrumentedMcpClient.test.ts
@src/server/runManager.ts
@src/server/storage/sqlite.ts
@src/server/storage/sqlite.test.ts
@src/server/api/history.ts
@src/server/api/history.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Promote instrumented tool traces and real screenshots into persisted step evidence</name>
  <files>src/server/orchestrator/types.ts, src/server/mcp/InstrumentedMcpClient.ts, src/server/mcp/InstrumentedMcpClient.test.ts, src/server/runManager.ts</files>
  <read_first>src/server/orchestrator/types.ts, src/server/mcp/InstrumentedMcpClient.ts, src/server/mcp/InstrumentedMcpClient.test.ts, src/server/runManager.ts, src/server/storage/screenshots.ts, .planning/phases/12-mejorar-fiabilidad-seguridad-y-transparencia-del-ejecutor-de-tests-para-qa/12-CONTEXT.md</read_first>
  <action>Unify the server-side `ToolCallTrace` shape so `src/server/orchestrator/types.ts` includes the exact fields already captured by instrumentation: `toolId`, `toolName`, `arguments`, `status`, `correlationId`, `latencyMs`, `captureTimestamp`, `screenshotId`, `result`, `error`, and `errorMessage`. Update `InstrumentedMcpClient` to expose step-ready traces in that shape, then change `trackStepResult()` in `src/server/runManager.ts` so the persisted `stepResult.toolCalls` comes from those enriched traces instead of lossy values. Remove the placeholder `getPlaceholderScreenshot()` path; use a real screenshot from instrumented evidence when available, and when no screenshot exists set a degraded reason such as `missing_step_screenshot` instead of silently fabricating one.</action>
  <verify>
    <automated>node ./node_modules/tsx/dist/cli.mjs --test "src/server/mcp/InstrumentedMcpClient.test.ts"</automated>
  </verify>
  <acceptance_criteria>`src/server/orchestrator/types.ts` declares `correlationId`, `captureTimestamp`, and `screenshotId`; `src/server/runManager.ts` does not contain the literal comment `1x1 transparent PNG`; `src/server/runManager.ts` records a degraded reason when screenshot evidence is absent; the InstrumentedMcpClient test command exits 0.</acceptance_criteria>
  <done>Step evidence is now based on real captured traces and screenshots rather than placeholders.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend SQLite and history/export APIs with trust state and reproducible config metadata</name>
  <files>src/server/storage/sqlite.ts, src/server/storage/sqlite.test.ts, src/server/api/history.ts, src/server/api/history.test.ts, src/server/runManager.ts</files>
  <read_first>src/server/storage/sqlite.ts, src/server/storage/sqlite.test.ts, src/server/api/history.ts, src/server/api/history.test.ts, src/server/runManager.ts, .planning/phases/12-mejorar-fiabilidad-seguridad-y-transparencia-del-ejecutor-de-tests-para-qa/12-CONTEXT.md</read_first>
  <action>Add run-level columns in SQLite with these exact names: `trustState TEXT NOT NULL DEFAULT 'auditable'`, `trustReasons TEXT NOT NULL DEFAULT '[]'`, `provider TEXT`, `orchestratorModel TEXT`, `lowCostAuditorModel TEXT`, and `highAccuracyAuditorModel TEXT`. Update `PersistedRun`, `RunDetail`, `saveRun()`, `listRuns()`, and `getRun()` so the JSON API returns those fields and `/api/history/:id/export.csv` plus `/api/history/export.csv` include trust/config columns. In `runManager.ts`, compute `trustState` as `degraded` whenever evidence is incomplete, assertions are unsupported for auto-execution, or validation falls back to review-only behavior; otherwise persist `auditable`.</action>
  <verify>
    <automated>node ./node_modules/tsx/dist/cli.mjs --test "src/server/storage/sqlite.test.ts" "src/server/api/history.test.ts"</automated>
    <automated>node ./node_modules/typescript/bin/tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>`src/server/storage/sqlite.ts` contains the exact column names `trustState`, `trustReasons`, `provider`, `orchestratorModel`, `lowCostAuditorModel`, and `highAccuracyAuditorModel`; `src/server/api/history.ts` CSV builders include `trustState` and `provider`; `tsx --test` for sqlite/history passes; `tsc --noEmit` exits 0.</acceptance_criteria>
  <done>Persisted runs and exports carry durable trust and execution-config metadata that QA can audit later.</done>
</task>

</tasks>

<verification>
`node ./node_modules/tsx/dist/cli.mjs --test "src/server/mcp/InstrumentedMcpClient.test.ts" "src/server/storage/sqlite.test.ts" "src/server/api/history.test.ts"` and `node ./node_modules/typescript/bin/tsc --noEmit` pass; manual source inspection confirms placeholder screenshots are gone and SQLite/API now expose trust/config fields.
</verification>

<success_criteria>
- Runs persist a trustworthy evidence trail with real screenshot linkage and rich per-tool-call traces.
- Missing evidence degrades trust explicitly instead of being hidden behind placeholders.
- History and exports expose stored trust state and resolved execution config.
</success_criteria>

<output>
After completion, create `.planning/phases/12-mejorar-fiabilidad-seguridad-y-transparencia-del-ejecutor-de-tests-para-qa/12-03-SUMMARY.md`
</output>
