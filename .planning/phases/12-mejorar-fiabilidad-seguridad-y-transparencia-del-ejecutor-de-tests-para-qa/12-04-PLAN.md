---
phase: 12-mejorar-fiabilidad-seguridad-y-transparencia-del-ejecutor-de-tests-para-qa
plan: 04
type: execute
wave: 3
depends_on: [12-02, 12-03]
files_modified:
  - src/client/App.tsx
  - src/client/types/history.ts
  - src/client/components/run/RunScorecard.tsx
  - src/client/components/history/RunDetailView.tsx
  - src/client/components/history/RunDetailView.phase12.contract.test.ts
  - src/client/components/run/RunScorecard.phase12.contract.test.ts
autonomous: true
requirements: [UI-04, UI-05, HIST-03]
must_haves:
  truths:
    - "La UI en vivo y el historial usan los labels exactos `AUDITABLE` y `DEGRADED` para que QA entienda la confianza del run sin leer logs."
    - "RunScorecard y RunDetailView muestran provider, orchestrator model, low-cost auditor, high-accuracy auditor, y las razones de degradacion persistidas."
    - "El detalle del run permite inspeccionar tool trace metadata persistida por paso: `toolName`, `status`, `latencyMs`, `correlationId`, y `screenshotId` cuando exista."
  artifacts:
    - path: "src/client/types/history.ts"
      provides: "Tipos frontend alineados con trust/config/evidence metadata nueva"
    - path: "src/client/App.tsx"
      provides: "Estado en vivo del run con badges de confianza y config visible"
    - path: "src/client/components/run/RunScorecard.tsx"
      provides: "Scorecard final con resumen de confianza, config y evidence quality"
    - path: "src/client/components/history/RunDetailView.tsx"
      provides: "Historial detallado con trust reasons y tool traces por paso"
  key_links:
    - from: "src/client/types/history.ts"
      to: "src/client/components/history/RunDetailView.tsx"
      via: "RunDetail"
      pattern: "trustState|trustReasons|provider|orchestratorModel"
    - from: "src/client/App.tsx"
      to: "src/client/components/run/RunScorecard.tsx"
      via: "live run state and stepEvidence"
      pattern: "AUDITABLE|DEGRADED"
    - from: "src/client/components/history/RunDetailView.tsx"
      to: "src/client/components/run/RunScorecard.tsx"
      via: "shared trust/config labels"
      pattern: "Provider|Orchestrator|Low-cost auditor|High-accuracy auditor"
---

<objective>
Surface trust state, degradation reasons, and reproducible execution config clearly in the live UI and history views so QA can judge result reliability without inspecting server logs.

Purpose: complete the Phase 12 transparency loop from persisted backend data to human-readable frontend surfaces.
Output: live and historical trust badges, config panels, and per-step trace inspection in the React UI.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-mejorar-fiabilidad-seguridad-y-transparencia-del-ejecutor-de-tests-para-qa/12-CONTEXT.md
@src/client/App.tsx
@src/client/types/history.ts
@src/client/components/run/RunScorecard.tsx
@src/client/components/history/RunDetailView.tsx
@src/server/api/history.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Render live trust state and execution config in the New Run experience and final scorecard</name>
  <files>src/client/App.tsx, src/client/components/run/RunScorecard.tsx, src/client/components/run/RunScorecard.phase12.contract.test.ts</files>
  <read_first>src/client/App.tsx, src/client/components/run/RunScorecard.tsx, src/client/types/history.ts, .planning/phases/12-mejorar-fiabilidad-seguridad-y-transparencia-del-ejecutor-de-tests-para-qa/12-CONTEXT.md</read_first>
  <action>Extend App live-run state so it stores `executionConfig`, `trustState`, and `trustReasons` from the backend event payloads introduced in 12-02/12-03. In the live/new-run section and in `RunScorecard.tsx`, render a dedicated summary block with the exact labels `Trust`, `Provider`, `Orchestrator`, `Low-cost auditor`, and `High-accuracy auditor`. The trust badge text must be exactly `AUDITABLE` for healthy runs and `DEGRADED` when trust reasons exist. When degraded, show each persisted reason as a pill or inline list item under the badge. Add `src/client/components/run/RunScorecard.phase12.contract.test.ts` to assert those exact strings exist in the scorecard component source.</action>
  <verify>
    <automated>node ./node_modules/tsx/dist/cli.mjs --test "src/client/components/run/RunScorecard.phase12.contract.test.ts"</automated>
    <automated>node ./node_modules/vite/bin/vite.js build</automated>
  </verify>
  <acceptance_criteria>`src/client/components/run/RunScorecard.tsx` contains the exact strings `AUDITABLE`, `DEGRADED`, `Provider`, `Orchestrator`, `Low-cost auditor`, and `High-accuracy auditor`; the new scorecard contract test file exists; `vite build` exits 0.</acceptance_criteria>
  <done>The live run and final scorecard expose trust and configuration clearly to QA operators.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose persisted trust reasons and tool trace metadata in RunDetailView</name>
  <files>src/client/types/history.ts, src/client/components/history/RunDetailView.tsx, src/client/components/history/RunDetailView.phase12.contract.test.ts</files>
  <read_first>src/client/types/history.ts, src/client/components/history/RunDetailView.tsx, src/server/api/history.ts, .planning/phases/12-mejorar-fiabilidad-seguridad-y-transparencia-del-ejecutor-de-tests-para-qa/12-CONTEXT.md</read_first>
  <action>Extend `src/client/types/history.ts` so `PersistedRun`, `RunDetail`, and `PersistedStep.toolCalls` include the exact fields introduced by 12-03: `trustState`, `trustReasons`, `provider`, `orchestratorModel`, `lowCostAuditorModel`, `highAccuracyAuditorModel`, and structured tool traces with `toolName`, `status`, `latencyMs`, `correlationId`, `screenshotId`, `result`, and `error`. In `RunDetailView.tsx`, add a trust/config summary panel near the existing metrics and add a per-step tool trace section that lists each tool call with name, status, latency, correlation ID, and screenshot link when `toolCall.screenshotId` exists. Add `src/client/components/history/RunDetailView.phase12.contract.test.ts` asserting the exact labels `Trust`, `Provider`, `Orchestrator`, `Low-cost auditor`, `High-accuracy auditor`, and `Correlation ID` appear in the component source.</action>
  <verify>
    <automated>node ./node_modules/tsx/dist/cli.mjs --test "src/client/components/history/RunDetailView.phase12.contract.test.ts"</automated>
    <automated>node ./node_modules/typescript/bin/tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>`src/client/types/history.ts` defines the trust/config fields; `src/client/components/history/RunDetailView.tsx` contains the label `Correlation ID`; the detail-view contract test file exists and passes; `tsc --noEmit` exits 0.</acceptance_criteria>
  <done>Run history becomes a durable audit surface instead of a screenshot-only recap.</done>
</task>

</tasks>

<verification>
`node ./node_modules/tsx/dist/cli.mjs --test "src/client/components/run/RunScorecard.phase12.contract.test.ts" "src/client/components/history/RunDetailView.phase12.contract.test.ts"`, `node ./node_modules/typescript/bin/tsc --noEmit`, and `node ./node_modules/vite/bin/vite.js build` pass; manual UI review confirms trust badges, config labels, degradation reasons, and tool trace metadata are visible in both live and historical views.
</verification>

<success_criteria>
- QA can see whether a run is `AUDITABLE` or `DEGRADED` without consulting logs.
- Stored execution config is visible in both the live and historical UI.
- History detail exposes step-level tool trace metadata and screenshot linkage for audit review.
</success_criteria>

<output>
After completion, create `.planning/phases/12-mejorar-fiabilidad-seguridad-y-transparencia-del-ejecutor-de-tests-para-qa/12-04-SUMMARY.md`
</output>
