---
phase: 13-excelencia-y-estabilizacion-de-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - README.md
  - scripts/test/run-lane.mjs
  - scripts/test/test-manifest.mjs
autonomous: true
requirements:
  - "Eliminacion del flakiness estabilizando aserciones, waits y gestion del estado global"
  - "Reduccion del tiempo de ejecucion con paralelizacion segura, mocking eficiente y setups/teardowns optimizados"
must_haves:
  truths:
    - "`npm test` only exercises the fast deterministic lane by default."
    - "A named smoke lane exists for slower persistence and integration coverage instead of inflating the routine path."
    - "The lane split is explicit in repo scripts and docs, not hidden in tribal knowledge."
  artifacts:
    - path: "scripts/test/run-lane.mjs"
      provides: "Single entrypoint for lane-aware test execution and listing"
    - path: "scripts/test/test-manifest.mjs"
      provides: "Source of truth for which files belong to fast versus smoke coverage"
    - path: "package.json"
      provides: "Developer-facing scripts for fast, smoke, and CI test execution"
  key_links:
    - from: "package.json"
      to: "scripts/test/run-lane.mjs"
      via: "npm test, npm run test:fast, npm run test:smoke, npm run test:ci"
      pattern: "run-lane\\.mjs"
    - from: "scripts/test/test-manifest.mjs"
      to: "README.md"
      via: "Documented lane policy and file ownership"
      pattern: "smoke"
---

<objective>
Establish the Phase 13 testing foundation: explicit fast and smoke lanes with one manifest-driven entrypoint.

Purpose: Protect everyday speed and determinism per D-04, D-05, D-07, and D-14 before changing the test portfolio itself.
Output: Lane runner scripts, package scripts, and README guidance that make the suite split executable and obvious.
</objective>

<execution_context>
@C:/Users/josoj/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/josoj/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-excelencia-y-estabilizacion-de-testing/13-CONTEXT.md
@package.json
@README.md

<interfaces>
From [package.json](/C:/INFORMATICA/MCP-Playwright/package.json):
```json
{
  "scripts": {
    "test": "node ./node_modules/tsx/dist/cli.mjs --test \"src/**/*.test.ts\""
  }
}
```

Current slow-leaning runtime tests called out by Phase 13 context:
```text
src/server/api/history.test.ts
src/server/storage/sqlite.test.ts
src/server/storage/screenshots.test.ts
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the lane runner and manifest contracts</name>
  <read_first>
    - package.json
    - README.md
    - .planning/phases/13-excelencia-y-estabilizacion-de-testing/13-CONTEXT.md
  </read_first>
  <files>package.json, scripts/test/run-lane.mjs, scripts/test/test-manifest.mjs</files>
  <action>Create `scripts/test/test-manifest.mjs` as the source of truth for lane ownership: a fast lane that includes deterministic `*.test.ts`, `*.contract.test.ts`, and `*.evidence.test.ts` coverage, and a smoke lane that explicitly lists slower real-I/O files. Create `scripts/test/run-lane.mjs` to resolve the manifest, print `--list` output, and spawn `node ./node_modules/tsx/dist/cli.mjs --test ...` with the concrete file set. Update `package.json` so `npm test` and `npm run test:fast` hit the fast lane, `npm run test:smoke` hits the slower lane, and `npm run test:ci` runs both lanes in order. Keep naming and arguments explicit per D-04, D-05, and D-06; do not leave discovery implicit in a single broad glob.</action>
  <acceptance_criteria>
    - `package.json` contains `test`, `test:fast`, `test:smoke`, and `test:ci` scripts that all route through `scripts/test/run-lane.mjs`.
    - `scripts/test/test-manifest.mjs` exposes an explicit fast/smoke ownership map instead of relying only on one wildcard glob.
    - `node scripts/test/run-lane.mjs fast --list` and `node scripts/test/run-lane.mjs smoke --list` each print a deterministic file list and exit without running tests.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/test/run-lane.mjs fast --list</automated>
  </verify>
  <done>The repo has executable lane entrypoints and `npm test` no longer means “run every test file indiscriminately”.</done>
</task>

<task type="auto">
  <name>Task 2: Document the lane policy and seed the initial smoke inventory</name>
  <read_first>
    - README.md
    - .planning/phases/13-excelencia-y-estabilizacion-de-testing/13-CONTEXT.md
    - src/server/api/history.test.ts
    - src/server/storage/sqlite.test.ts
    - src/server/storage/screenshots.test.ts
  </read_first>
  <files>README.md, scripts/test/test-manifest.mjs</files>
  <action>Update README testing guidance to describe the two-lane model, when each lane should run, and how CI should consume them. Seed `scripts/test/test-manifest.mjs` with the current real storage/API tests called out in the Phase 13 context as the initial smoke inventory, so the fast lane is immediately protected from slow I/O drift even before later plans rename or deepen those files. Tie the documentation back to D-07, D-08, D-13, and D-14: fast lane for isolated routine checks, smoke lane for a small set of real seams.</action>
  <acceptance_criteria>
    - README has a dedicated test section describing the default fast lane and the secondary smoke lane with concrete commands.
    - The manifest explicitly places `src/server/api/history.test.ts`, `src/server/storage/sqlite.test.ts`, and `src/server/storage/screenshots.test.ts` in the smoke inventory.
    - `npm test` excludes the seeded smoke inventory while `npm run test:smoke` includes it.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/test/run-lane.mjs smoke --list</automated>
  </verify>
  <done>Lane ownership is documented and the current slow real-I/O tests are carved out of the default path.</done>
</task>

</tasks>

<verification>
Run the lane listing commands and confirm the fast lane omits the seeded smoke files while the smoke lane contains them. Then run `npm test` to prove the default entrypoint still works through the new runner.
</verification>

<success_criteria>
`npm test` becomes the fast deterministic safety lane, `npm run test:smoke` becomes the explicit slower lane, and the repo documents the split clearly enough that later plans can move tests without changing the policy again.
</success_criteria>

<output>
After completion, create `.planning/phases/13-excelencia-y-estabilizacion-de-testing/13-01-SUMMARY.md`
</output>
