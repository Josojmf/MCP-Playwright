---
phase: 13-excelencia-y-estabilizacion-de-testing
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - src/test/support/sourceContracts.ts
  - src/server/runManager.phase12.evidence.test.ts
  - src/server/runManager.phase8.contract.test.ts
  - src/cli/mcp-bench.phase10.contract.test.ts
  - src/client/App.phase12.contract.test.ts
  - src/client/components/run/RunScorecard.phase12.contract.test.ts
  - src/client/components/history/RunDetailView.phase12.contract.test.ts
autonomous: true
requirements:
  - "Auditoria de tests actuales frente a la implementacion real para detectar y cerrar escenarios no cubiertos"
  - "Eliminacion del flakiness estabilizando aserciones, waits y gestion del estado global"
must_haves:
  truths:
    - "Source-shape contract tests still exist, but only for architectural invariants that are expensive to miss."
    - "Weak regex-only guards around incidental labels or formatting no longer carry confidence by themselves."
    - "Contract assertions are reusable and explainable instead of ad-hoc regex blobs duplicated across files."
  artifacts:
    - path: "src/test/support/sourceContracts.ts"
      provides: "Shared helpers for structural source-contract assertions"
    - path: "src/server/runManager.phase12.evidence.test.ts"
      provides: "High-value contract guard for execution config and evidence persistence invariants"
    - path: "src/cli/mcp-bench.phase10.contract.test.ts"
      provides: "Contract guard for CLI trace formatting invariants only"
  key_links:
    - from: "src/test/support/sourceContracts.ts"
      to: "src/server/runManager.phase12.evidence.test.ts"
      via: "Shared structural assertions"
      pattern: "sourceContracts"
    - from: "src/test/support/sourceContracts.ts"
      to: "src/client/App.phase12.contract.test.ts"
      via: "JSX/source invariant checks without brittle label-only regex"
      pattern: "sourceContracts"
---

<objective>
Prune and strengthen the contract layer so it protects architecture, wiring, and non-regression seams instead of incidental source text.

Purpose: Honor D-01 and D-02 by keeping source-shape tests as first-class coverage while reducing brittle regex-only assertions that create false confidence.
Output: Shared contract helpers plus a smaller, higher-value set of contract tests around the critical runtime and CLI/UI invariants.
</objective>

<execution_context>
@C:/Users/josoj/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/josoj/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-excelencia-y-estabilizacion-de-testing/13-CONTEXT.md
@src/server/runManager.phase12.evidence.test.ts
@src/server/runManager.phase8.contract.test.ts
@src/cli/mcp-bench.phase10.contract.test.ts
@src/client/App.phase12.contract.test.ts
@src/client/components/run/RunScorecard.phase12.contract.test.ts
@src/client/components/history/RunDetailView.phase12.contract.test.ts

<interfaces>
From [src/server/runManager.ts](/C:/INFORMATICA/MCP-Playwright/src/server/runManager.ts):
```ts
export interface RunEstimateRequest
export interface RunExecutionConfig
export class PhaseOneRunManager
```

From [src/cli/mcp-bench.ts](/C:/INFORMATICA/MCP-Playwright/src/cli/mcp-bench.ts):
```ts
function runDebug(args: Record<string, string>): number
function truncateText(value: string, maxLength: number): string
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce shared structural contract helpers</name>
  <read_first>
    - src/server/runManager.phase12.evidence.test.ts
    - src/server/runManager.phase8.contract.test.ts
    - src/cli/mcp-bench.phase10.contract.test.ts
    - src/client/App.phase12.contract.test.ts
  </read_first>
  <files>src/test/support/sourceContracts.ts</files>
  <action>Create `src/test/support/sourceContracts.ts` using the existing `typescript` dev dependency to parse source files and expose focused helpers for exported interface/property presence, function-call object properties, JSX label checks, and normalized string fragments when AST inspection is not practical. The helpers must let contract tests assert invariants like “executionConfig includes provider/models” or “runDebug formats tool-call lines” without large free-form regex blocks. Per D-02, document inside the helper when string matching is still allowed and keep it as a narrow fallback, not the default mechanism.</action>
  <acceptance_criteria>
    - `src/test/support/sourceContracts.ts` exists and exports reusable helpers consumed by multiple contract tests.
    - The helper uses `typescript` AST or tokenized source inspection for structural checks rather than only `assert.match` on raw file strings.
    - Any remaining regex fallback is isolated behind helper functions with comments explaining why AST lookup is insufficient.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>A reusable contract-testing toolkit exists so later assertions no longer need bespoke regex blobs in each phase-tagged test file.</done>
</task>

<task type="auto">
  <name>Task 2: Rebalance the retained contract tests around invariants instead of incidental text</name>
  <read_first>
    - src/test/support/sourceContracts.ts
    - src/server/runManager.phase12.evidence.test.ts
    - src/server/runManager.phase8.contract.test.ts
    - src/cli/mcp-bench.phase10.contract.test.ts
    - src/client/App.phase12.contract.test.ts
    - src/client/components/run/RunScorecard.phase12.contract.test.ts
    - src/client/components/history/RunDetailView.phase12.contract.test.ts
  </read_first>
  <files>src/server/runManager.phase12.evidence.test.ts, src/server/runManager.phase8.contract.test.ts, src/cli/mcp-bench.phase10.contract.test.ts, src/client/App.phase12.contract.test.ts, src/client/components/run/RunScorecard.phase12.contract.test.ts, src/client/components/history/RunDetailView.phase12.contract.test.ts</files>
  <action>Audit the existing phase-tagged contract tests against the actual Phase 13 policy. Upgrade the high-value guards on `runManager`, CLI debug formatting, and App run-config wiring to use the new helper and assert architectural invariants. Remove or fold weaker tests whose only value is checking literal labels like `Trust`, `Provider`, `Config:`, or CSS-class text without protecting wiring or required behavior. If a UI contract remains, anchor it to data-flow invariants such as the trust/config fields being rendered from the live payload rather than to exact literal formatting. Reference D-01, D-02, and D-03 explicitly in comments or summary notes when deleting or rewriting a contract test.</action>
  <acceptance_criteria>
    - Retained contract tests use `src/test/support/sourceContracts.ts` instead of duplicated raw regex patterns for their main assertions.
    - Contract tests that remain are clearly tied to architectural invariants, required wiring, or expensive non-regression points.
    - Literal-label-only guards are deleted or downgraded when equivalent confidence is provided by stronger contract or behavioral coverage.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:fast</automated>
  </verify>
  <done>The contract layer is smaller, structurally stronger, and no longer spends most of its assertions on incidental source text.</done>
</task>

</tasks>

<verification>
Run the fast lane and inspect the remaining contract tests. They should now read as invariant guards, not implementation-text snapshots.
</verification>

<success_criteria>
The source-shape suite remains first-class coverage, but it now focuses on structural invariants for `runManager`, CLI debug formatting, and critical UI wiring instead of weak regex-only guards.
</success_criteria>

<output>
After completion, create `.planning/phases/13-excelencia-y-estabilizacion-de-testing/13-02-SUMMARY.md`
</output>
