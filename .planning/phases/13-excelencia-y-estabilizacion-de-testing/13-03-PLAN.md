---
phase: 13-excelencia-y-estabilizacion-de-testing
plan: 03
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - src/test/support/runtimeFixtures.ts
  - src/server/runManager.test.ts
  - src/server/orchestrator/OrchestratorService.test.ts
  - src/cli/mcp-bench.ts
  - src/cli/mcp-bench.test.ts
autonomous: true
requirements:
  - "Auditoria de tests actuales frente a la implementacion real para detectar y cerrar escenarios no cubiertos"
  - "Eliminacion del flakiness estabilizando aserciones, waits y gestion del estado global"
must_haves:
  truths:
    - "Core runtime confidence comes primarily from executable behavioral tests, not phase-tagged source inspection."
    - "runManager, OrchestratorService, and CLI paths can be exercised with deterministic fixtures and no shared global leakage."
    - "Fast-lane failures point to behavior regressions in the important runtime surfaces called out by Phase 13."
  artifacts:
    - path: "src/test/support/runtimeFixtures.ts"
      provides: "Reusable deterministic fixtures and stubs for fast behavioral tests"
    - path: "src/server/runManager.test.ts"
      provides: "Executable coverage for runManager request normalization and execution-config behavior"
    - path: "src/cli/mcp-bench.test.ts"
      provides: "Behavioral CLI coverage for run and debug flows"
  key_links:
    - from: "src/test/support/runtimeFixtures.ts"
      to: "src/server/orchestrator/OrchestratorService.test.ts"
      via: "Deterministic provider/tool-client fixtures"
      pattern: "runtimeFixtures"
    - from: "src/cli/mcp-bench.test.ts"
      to: "src/cli/mcp-bench.ts"
      via: "Exported or injectable CLI helpers tested without shell flake"
      pattern: "runHeadless|runDebug|parseArgs"
---

<objective>
Shift confidence toward fast executable behavioral coverage for the highest-value runtime surfaces.

Purpose: Implement D-03, D-07, D-08, and D-09 by strengthening deterministic tests around runManager, OrchestratorService, and CLI instead of leaning on source inspection for those paths.
Output: Shared fast-lane fixtures plus improved behavioral tests for runManager, OrchestratorService, and CLI behavior.
</objective>

<execution_context>
@C:/Users/josoj/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/josoj/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-excelencia-y-estabilizacion-de-testing/13-CONTEXT.md
@src/server/runManager.test.ts
@src/server/orchestrator/OrchestratorService.test.ts
@src/cli/mcp-bench.ts

<interfaces>
From [src/server/orchestrator/OrchestratorService.ts](/C:/INFORMATICA/MCP-Playwright/src/server/orchestrator/OrchestratorService.ts):
```ts
export class OrchestratorService
```

From [src/cli/mcp-bench.ts](/C:/INFORMATICA/MCP-Playwright/src/cli/mcp-bench.ts):
```ts
async function runHeadless(args: Record<string, string>): Promise<number>
function runDebug(args: Record<string, string>): number
function parseArgs(rawArgs: string[]): Record<string, string>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deterministic fixture support for fast behavioral runtime tests</name>
  <read_first>
    - src/server/runManager.test.ts
    - src/server/orchestrator/OrchestratorService.test.ts
    - .planning/phases/13-excelencia-y-estabilizacion-de-testing/13-CONTEXT.md
  </read_first>
  <files>src/test/support/runtimeFixtures.ts, src/server/runManager.test.ts, src/server/orchestrator/OrchestratorService.test.ts</files>
  <action>Create `src/test/support/runtimeFixtures.ts` with reusable fake providers, fake tool clients, run-context builders, fixed timestamps, and isolated temp-state helpers for the fast lane. Use it to expand `runManager.test.ts` and `OrchestratorService.test.ts` around the core runtime seams named in the phase focus: request normalization, explicit execution-config defaults, abort/error semantics, token-budget failures, tool-loop behavior, and assertion override behavior. Per D-07 and D-09, redesign test setup to avoid shared global state and timing sensitivity instead of papering over flake with retries or loose waits.</action>
  <acceptance_criteria>
    - `src/test/support/runtimeFixtures.ts` centralizes shared fast-lane fixtures instead of duplicating ad-hoc stubs in multiple tests.
    - `runManager.test.ts` and `OrchestratorService.test.ts` each gain executable assertions for Phase 12/13-critical behavior, not only parsing or trivial status checks.
    - The new tests do not rely on real filesystem, SQLite, or child-process side effects that belong in the smoke lane.
  </acceptance_criteria>
  <verify>
    <automated>node ./node_modules/tsx/dist/cli.mjs --test src/server/runManager.test.ts src/server/orchestrator/OrchestratorService.test.ts</automated>
  </verify>
  <done>The fast lane has stronger behavioral coverage on the most important backend execution paths with deterministic reusable fixtures.</done>
</task>

<task type="auto">
  <name>Task 2: Add executable CLI behavior tests and minimal testability refactors</name>
  <read_first>
    - src/cli/mcp-bench.ts
    - src/cli/mcp-bench.phase6.contract.test.ts
    - src/cli/mcp-bench.phase8.contract.test.ts
    - src/cli/mcp-bench.phase10.contract.test.ts
  </read_first>
  <files>src/cli/mcp-bench.ts, src/cli/mcp-bench.test.ts</files>
  <action>Create `src/cli/mcp-bench.test.ts` to exercise the CLI behavior directly: provider validation, `anthropic -> claude` mapping, structured JSON output, exit-code logic, debug filtering, and `[HALLUCINATED]` / `[NEEDS-REVIEW]` formatting. If needed, make the smallest production refactor in `src/cli/mcp-bench.ts` to export pure helpers or inject dependencies for testing, but do not change public CLI behavior or add new product capabilities. This plan should reduce reliance on phase-tagged source contracts for CLI confidence per D-03 while keeping the fast lane deterministic.</action>
  <acceptance_criteria>
    - `src/cli/mcp-bench.test.ts` exists and covers both `run` and `debug` behavior with executable assertions.
    - Any production refactor in `src/cli/mcp-bench.ts` is limited to testability seams such as exported helpers or dependency injection, with no behavior change to the CLI contract.
    - CLI confidence no longer depends primarily on source-shape regex tests.
  </acceptance_criteria>
  <verify>
    <automated>node ./node_modules/tsx/dist/cli.mjs --test src/cli/mcp-bench.test.ts</automated>
  </verify>
  <done>The CLI has real behavioral tests in the fast lane, and confidence comes from execution rather than source inspection.</done>
</task>

</tasks>

<verification>
Run the targeted fast behavioral tests and then the full fast lane. Confirm the high-value runtime surfaces from the phase brief are exercised without real persistence or process flake.
</verification>

<success_criteria>
runManager, OrchestratorService, and CLI regressions are caught by deterministic behavioral tests in the fast lane, and the suite’s confidence burden shifts away from brittle source-text assertions.
</success_criteria>

<output>
After completion, create `.planning/phases/13-excelencia-y-estabilizacion-de-testing/13-03-SUMMARY.md`
</output>
