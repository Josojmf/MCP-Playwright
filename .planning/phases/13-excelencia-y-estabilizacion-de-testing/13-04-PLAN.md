---
phase: 13-excelencia-y-estabilizacion-de-testing
plan: 04
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - scripts/test/run-lane.mjs
  - scripts/test/test-manifest.mjs
  - src/test/support/runtimeSmokeHarness.ts
  - src/test/support/failureBundle.ts
  - src/server/api/history.smoke.test.ts
  - src/server/storage/sqlite.smoke.test.ts
  - src/server/storage/screenshots.smoke.test.ts
autonomous: true
requirements:
  - "Reduccion del tiempo de ejecucion con paralelizacion segura, mocking eficiente y setups/teardowns optimizados"
  - "Reporting enriquecido con logs, trazas, capturas y reportes legibles para debugging rapido en CI/CD"
must_haves:
  truths:
    - "A small real smoke lane still validates high-risk persistence/filesystem seams against reality."
    - "Important lanes emit structured failure bundles with enough context to debug without rerunning locally."
    - "Screenshot-first diagnostics are preserved for smoke failures, with heavier capture remaining opt-in."
  artifacts:
    - path: "src/test/support/failureBundle.ts"
      provides: "Structured bundle writer for smoke-lane failures"
    - path: "src/test/support/runtimeSmokeHarness.ts"
      provides: "Isolated temp-dir and cleanup harness for real smoke tests"
    - path: "src/server/api/history.smoke.test.ts"
      provides: "Smoke coverage for persisted history/export behavior with bundle-aware diagnostics"
  key_links:
    - from: "scripts/test/run-lane.mjs"
      to: "src/test/support/failureBundle.ts"
      via: "Smoke-lane artifact directory creation and bundle finalization"
      pattern: "failure"
    - from: "src/test/support/runtimeSmokeHarness.ts"
      to: "src/server/storage/screenshots.smoke.test.ts"
      via: "Temp data-dir registration and screenshot artifact retention on failure"
      pattern: "runtimeSmokeHarness"
---

<objective>
Preserve a small real smoke suite and enrich it with CI-friendly failure bundles.

Purpose: Implement D-08, D-10, D-11, D-12, D-15, and D-16 by keeping a limited set of real persistence/filesystem seams while making their failures diagnosable from structured artifacts.
Output: Smoke test harness, failure bundle writer, renamed smoke tests, and lane-runner support for artifact output.
</objective>

<execution_context>
@C:/Users/josoj/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/josoj/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-excelencia-y-estabilizacion-de-testing/13-CONTEXT.md
@scripts/test/run-lane.mjs
@scripts/test/test-manifest.mjs
@src/server/api/history.test.ts
@src/server/storage/sqlite.test.ts
@src/server/storage/screenshots.test.ts
@src/server/storage/screenshots.ts
@src/server/storage/sqlite.ts
@src/server/api/history.ts

<interfaces>
From [src/server/storage/screenshots.ts](/C:/INFORMATICA/MCP-Playwright/src/server/storage/screenshots.ts):
```ts
export async function saveScreenshot(buffer: Buffer, runId: string, stepId: string, dataDir?: string, toolCallId?: string): Promise<string>
export async function getScreenshot(screenshotId: string, dataDir?: string): Promise<Buffer | undefined>
export async function listScreenshotsByStep(runId: string, stepId: string, dataDir?: string): Promise<ScreenshotMetadata[]>
```

From [src/server/storage/sqlite.ts](/C:/INFORMATICA/MCP-Playwright/src/server/storage/sqlite.ts):
```ts
export function saveRun(...)
export function getRun(runId: string): RunDetail | null
export function closeDb(): void
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the smoke harness and structured failure bundle pipeline</name>
  <read_first>
    - scripts/test/run-lane.mjs
    - scripts/test/test-manifest.mjs
    - .planning/phases/13-excelencia-y-estabilizacion-de-testing/13-CONTEXT.md
  </read_first>
  <files>scripts/test/run-lane.mjs, scripts/test/test-manifest.mjs, src/test/support/runtimeSmokeHarness.ts, src/test/support/failureBundle.ts</files>
  <action>Create `src/test/support/runtimeSmokeHarness.ts` for isolated temp directories, `DATA_DIR` scoping, SQLite reset/close handling, and cleanup registration for smoke tests. Create `src/test/support/failureBundle.ts` to write structured bundles under a stable path such as `.artifacts/test-failures/<lane>/<timestamp>/`, including `manifest.json`, stdout/stderr captures, timing, failing test files, env summary, and registered artifact directories. Extend `scripts/test/run-lane.mjs` so the smoke lane can opt into bundle output and summarize bundle paths on failure. Keep screenshots as the default retained artifact per D-15 and D-16; make video absent by default unless a later test explicitly opts in.</action>
  <acceptance_criteria>
    - Smoke tests have a shared harness for temp directories, database resets, and cleanup rather than ad-hoc setup/teardown blocks.
    - Smoke-lane failures write a structured bundle directory with machine-readable metadata plus captured logs.
    - The lane runner prints the generated bundle path when the smoke lane fails.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/test/run-lane.mjs smoke --list</automated>
  </verify>
  <done>The smoke lane has a reusable isolation harness and a CI-friendly structured failure-bundle mechanism.</done>
</task>

<task type="auto">
  <name>Task 2: Move the real persistence/filesystem tests into smoke ownership and wire bundle-aware diagnostics</name>
  <read_first>
    - src/test/support/runtimeSmokeHarness.ts
    - src/test/support/failureBundle.ts
    - src/server/api/history.test.ts
    - src/server/storage/sqlite.test.ts
    - src/server/storage/screenshots.test.ts
  </read_first>
  <files>src/server/api/history.smoke.test.ts, src/server/storage/sqlite.smoke.test.ts, src/server/storage/screenshots.smoke.test.ts, scripts/test/test-manifest.mjs</files>
  <action>Rename the existing real API/persistence/filesystem tests into explicit `*.smoke.test.ts` ownership and refactor them onto `runtimeSmokeHarness.ts`. For each file, register the temp data directory, persisted screenshot paths, and any exported JSON/CSV or Fastify response bodies with `failureBundle.ts` so failures preserve the evidence that mattered. Keep the scope tight per D-08: a small number of real smoke tests for SQLite, screenshot storage, and history/export seams. Avoid adding broad new end-to-end flows or always-on heavy capture.</action>
  <acceptance_criteria>
    - `history`, `sqlite`, and `screenshots` real-I/O tests live under explicit `*.smoke.test.ts` paths and are owned by the smoke lane manifest.
    - Each smoke file uses the shared harness and registers relevant artifacts for bundle capture on failure.
    - The fast lane no longer includes these real-I/O tests, while the smoke lane does.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:smoke</automated>
  </verify>
  <done>The real persistence/filesystem seams are covered by a small smoke suite with artifact-rich diagnostics and no longer slow down the default lane.</done>
</task>

</tasks>

<verification>
Run `npm run test:smoke`. If the lane fails during development, confirm the reported bundle path contains the saved logs and registered artifacts. If it passes, use the lane list to confirm the smoke ownership boundaries remain explicit.
</verification>

<success_criteria>
The suite keeps a small real smoke lane for the highest-risk seams and produces structured failure bundles with screenshot-first evidence, logs, and temp-state context suitable for CI debugging.
</success_criteria>

<output>
After completion, create `.planning/phases/13-excelencia-y-estabilizacion-de-testing/13-04-SUMMARY.md`
</output>
